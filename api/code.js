const config = require('../config.json');

module.exports = function(app) {
  // Nothing here for now because trying to get the npm and github
  // api stuff to work was taking too long and it doesn't change all
  // that much so for now the numbers are being hardcoded
}